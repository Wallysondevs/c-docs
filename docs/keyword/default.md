# Palavras-chave C: default

### Uso

  * [ instrução `switch`](<#/doc/language/switch>): como a declaração do rótulo de caso `default`

  * [ expressão genérica por tipo](<#/doc/language/generic>): como a declaração da associação genérica `default`

| (desde C11)